<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <solid android:color="@color/diskInner" />
    <stroke
        android:width="@dimen/disk_ring_size"
        android:color="@color/diskOuter" />

    <size
        android:width="@dimen/disk_size"
        android:height="@dimen/disk_size" />
</shape>
